<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         AnkiDroid bridge
         ============================================================ -->
    <uses-permission android:name="com.ichi2.anki.permission.READ_WRITE_DATABASE"/>

    <!-- ============================================================
         Notifications + lockscreen
         ============================================================ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- ============================================================
         Pacing engine
         ============================================================ -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- ============================================================
         Overlay + app blocking
         ============================================================ -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- ============================================================
         Package visibility (Android 11+)
         Without this, ContentResolver returns null for any
         FlashCardsContract URI even when the permission is granted.
         ============================================================ -->
    <queries>
        <package android:name="com.ichi2.anki"/>
        <package android:name="com.ichi2.anki.debug"/>
        <package android:name="com.ichi2.anki.a"/>
        <provider android:authorities="com.ichi2.anki.flashcards"/>
        <provider android:authorities="com.ichi2.anki.debug.flashcards"/>
        <intent>
            <action android:name="com.ichi2.anki.DO_SYNC"/>
        </intent>
    </queries>

    <application
        android:name=".app.FlashcardsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashcardsEverywhere"
        tools:targetApi="35">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.FlashcardsEverywhere">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Lockscreen / full-screen-intent reviewer -->
        <activity
            android:name=".surface.lockscreen.LockscreenReviewerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.FlashcardsEverywhere.Fullscreen"/>

        <!-- Notification grade buttons -->
        <receiver
            android:name=".surface.notification.GradeReceiver"
            android:exported="false"/>

        <!-- Glance widget -->
        <receiver
            android:name=".surface.widget.CardWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/card_widget_info"/>
        </receiver>

        <!-- Pacing foreground service -->
        <service
            android:name=".service.pacing.PacingService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Tracks screen-on time to pace flashcard delivery toward the user's daily review goal."/>
        </service>

        <!-- Boot receiver to restart pacing -->
        <receiver
            android:name=".service.pacing.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- App blocker foreground service (full flavor) -->
        <service
            android:name=".service.blocking.AppBlockerService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Blocks designated apps and requires flashcard review to unlock them."/>
        </service>

        <!-- Dream service (screensaver): flashcards when charging/idle -->
        <service
            android:name=".surface.dream.FlashcardDreamService"
            android:exported="true"
            android:permission="android.permission.BIND_DREAM_SERVICE">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data
                android:name="android.service.dream"
                android:resource="@xml/flashcard_dream_info"/>
        </service>

        <!-- Media session service: flashcard as media notification on lockscreen -->
        <service
            android:name=".surface.media.StudyMediaSessionService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Displays current flashcard as media notification on lockscreen for ambient review."/>
        </service>

        <!-- Bubble activity: hosted inside notification bubble -->
        <activity
            android:name=".surface.bubble.BubbleActivity"
            android:exported="false"
            android:allowEmbedded="true"
            android:resizeableActivity="true"
            android:documentLaunchMode="always"
            android:theme="@style/Theme.FlashcardsEverywhere"/>

    </application>
</manifest>
