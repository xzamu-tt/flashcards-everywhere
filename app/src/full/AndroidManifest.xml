<?xml version="1.0" encoding="utf-8"?>
<!--
    Full-flavor manifest overlay.

    Adds the AccessibilityService that draws the overlay on every foreground
    app change. NOT distributed via Play Store: Google's Jan 2026
    AccessibilityService policy bans non-assistive use.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <service
            android:name=".surface.overlay.CardOverlayAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>
    </application>
</manifest>
